# Documents for python

This application has two endpoints:

 - `python-app.test.com/api/v1info`
 - `python-app.test.com/api/healthz`

Here you could expand on what each of these endpoinst do.

# How to access the app?

You can access to app by accessing this URL: `python-app.test.com/api/healthz`